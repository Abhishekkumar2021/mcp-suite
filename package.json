{
  "name": "mcp-servers",
  "version": "0.0.0",
  "private": true,
  "description": "A collection of Model Context Protocol (MCP) servers.",
  "workspaces": [
    "servers/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "rm -rf servers/*/dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
